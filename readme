

1. Activar el entorno virtual de  python en vsCode 

2.Installar todas las librerias con los siguientes comando(preferible abir el cmd en modo administrador)

pip install SpeechRecognition

pip install PyAudio

pip install pywhatkit

pip install pyttsx3

pip install wikipedia

pip install keyboard

pip install os

pip install cam

pip install opencv-python

pip install subprocess.run

pip install pygame

pip install mixer

pip install tk

3.necesitas revisar los idiomas instalado, english, españa, mexico de acuerdo a su posicion, para ello puedes descomentar la linea 75 y 76 y revisar en consola el orden 